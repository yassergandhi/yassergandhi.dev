---
const before = [
  'the team names the same failure differently',
  'support keeps explaining what the product should have made legible',
  'onboarding compensates for weak product guidance',
  'customer success creates workarounds',
  'product keeps adjusting the wrong layer',
  'leadership sees symptoms without a shared diagnosis',
];

const after = [
  'the failure point is named',
  'the hidden product assumption is visible',
  'the cost transfer is mapped',
  'the responsible team can stop guessing',
  'the next scaffold is specified',
  'the first intervention becomes defendable',
];
---

<section id="before-after">
  <div class="container">
    <div class="section-label">Before / After</div>
    <h2>What changes after the review.</h2>
    <div class="friction-grid">
      <div class="project-card offer-card">
        <h3>Before the review</h3>
        <ul class="deliverable-list">
          {before.map((item) => <li>{item}</li>)}
        </ul>
      </div>
      <div class="project-card offer-card">
        <h3>After the review</h3>
        <ul class="deliverable-list">
          {after.map((item) => <li>{item}</li>)}
        </ul>
      </div>
    </div>
    <p class="section-copy cost-transfer-close">
      The review does not promise instant growth. It gives the team a shared diagnosis and the next scaffold to build.
    </p>
  </div>
</section>
